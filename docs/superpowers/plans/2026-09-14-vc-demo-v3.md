# VC Demo V3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce and merge a clear, production-backed Layak investor walkthrough that includes qualification evidence, Strategy, and Cik Lay within 120 seconds.

**Architecture:** Extend the existing named-beat contract so missing visual proof fails capture before narration. Keep production-specific browser actions in `walk.mjs`, derive speech and subtitles from the same measured beats, and retain the existing warm-up, Chatterbox, BGM, and ffmpeg pipeline.

**Tech Stack:** Node.js, Playwright, Python `unittest`, Chatterbox TTS, ffmpeg/libass, Bash

**Spec:** `docs/superpowers/specs/2026-09-14-vc-demo-v3.md`

## Global Constraints

- Use only synthetic Aisyah data and the existing persisted result.
- Film the deployed production site after cold-start warm-up.
- Keep the final runtime between 60 and 120 seconds.
- Retain Quicksand subtitles, cloned voice, ducked BGM, and 1920×1080 H.264/AAC delivery.
- Keep `AGENTS.md` under 200 lines.

---

### Task 1: Capture and presentation regressions

**Files:**

- Modify: `scripts/demo/tests/walk-contract.test.mjs`
- Modify: `scripts/demo/tests/test_narrate.py`
- Modify: `scripts/demo/tests/test_subtitles.py`

**Interfaces:**

- Consumes: `REQUIRED_BEATS`, `MIN_BEAT_INTERVAL_MS`, `narrate.sh`
- Produces: failing expectations for the approved beat order, 120-second ceiling, one-minute transition, and compact subtitle backing

- [x] **Step 1: Write failing capture-contract tests**

  Require this literal order:

  ```js
  ;[
    'landing',
    'guest',
    'dashboard',
    'intake',
    'processing',
    'transition',
    'results',
    'qualification',
    'evidence',
    'strategy',
    'ciklay_compact',
    'ciklay_expanded',
    'ciklay_answer',
    'whatif',
    'scenario',
    'packets',
    'end'
  ]
  ```

  Assert that the transition says about one minute, the qualification control
  is clicked, Strategy is targeted, and both compact and expanded Cik Lay
  controls are exercised.

- [x] **Step 2: Run the Node test and observe the old flow fail**

  Run: `node --test scripts/demo/tests/walk-contract.test.mjs`

- [x] **Step 3: Write failing runtime and subtitle-style tests**

  Change the expected default maximum to `120` and require libass box padding
  no greater than `1`.

- [x] **Step 4: Run the Python tests and observe the old defaults fail**

  Run: `python3 -m unittest scripts.demo.tests.test_narrate scripts.demo.tests.test_subtitles`

### Task 2: Implement the approved production choreography

**Files:**

- Modify: `scripts/demo/contract.mjs`
- Modify: `scripts/demo/walk.mjs`
- Modify: `scripts/demo/narration.txt`

**Interfaces:**

- Consumes: production accessible names, `smoothScrollTo`, capture `mark()`
- Produces: all 17 required visual beats with measured narration boundaries

- [x] **Step 1: Add the missing named beats and their voice-safe intervals**

  Add `qualification`, `strategy`, `ciklay_compact`, `ciklay_expanded`, and
  `ciklay_answer` to the contract and interval map.

- [x] **Step 2: Replace the transition claim**

  Render `About one minute later` and narrate that a full evaluation takes
  about a minute.

- [x] **Step 3: Film qualification and Strategy**

  Reveal the first scheme's `Tap to reveal` control, then open its Sources;
  scroll linearly to `#strategy` and hold on a visible advisory.

- [x] **Step 4: Film Cik Lay end to end**

  Click `Ask Cik Lay About This`, capture the compact dialog, click `Expand to
centre modal`, type a concrete JKM application-and-documents follow-up, send
  it, and mark the answer only after a substantive, relevant non-streaming
  model response is visible.

- [x] **Step 5: Preserve the what-if and packet ending**

  Close the dialog, linearly scroll to What-Ifs, wait for `RM 14,558`, then open
  the generated packet iframe.

- [x] **Step 6: Run the contract test and make it pass**

  Run: `node --test scripts/demo/tests/walk-contract.test.mjs`

### Task 3: Update the two-minute and subtitle presentation contract

**Files:**

- Modify: `scripts/demo/narrate.sh`
- Modify: `scripts/demo/README.md`
- Modify: `AGENTS.md`

**Interfaces:**

- Consumes: `DEMO_MAX_DURATION`, libass `force_style`
- Produces: a 120-second default gate and non-overlapping subtitle backing

- [x] **Step 1: Set the runtime ceiling to 120 seconds**

  Update the executable default and human-facing runbook examples from 75 to
  120 seconds, while retaining the 60-second minimum.

- [x] **Step 2: Reduce subtitle backing height**

  Keep `FontName=Quicksand`, `FontSize=10.5`, `MarginV=10`, and reduce `Outline`
  from `2` to no more than `1`.

- [x] **Step 3: Keep the repository agent guide concise**

  Change only the Demo Standard duration and confirm `AGENTS.md` remains below
  200 lines.

- [x] **Step 4: Run all demo unit tests**

  Run: `python3 -m unittest discover -s scripts/demo/tests -p 'test_*.py'`
  and `node --test scripts/demo/tests/walk-contract.test.mjs`.

### Task 4: Capture, dub, inspect, and deliver

**Files:**

- Produce outside git: `${DEMO_DIR}/capture.webm`, `${DEMO_DIR}/demo.mp4`
- Replace outside git: `~/Downloads/Layak-VC-Walkthrough.mp4`

**Interfaces:**

- Consumes: deployed Layak, Chatterbox reference WAV, lofi BGM
- Produces: verified investor MP4

- [x] **Step 1: Warm production and record**

  Run `record.mjs` with warm-up enabled and reject any missing named surface.

- [x] **Step 2: Render cloned narration, subtitles, and music**

  Run `narrate.sh` with `DEMO_TTS=chatterbox`, the approved reference WAV,
  BGM, and `DEMO_MAX_DURATION=120`.

- [x] **Step 3: Inspect representative frames**

  Extract frames for qualification, Strategy, compact Cik Lay, expanded Cik
  Lay, answer, a two-line subtitle, what-if, and packet; reject obscured UI,
  mistimed narration, or touching subtitle backing rows.

- [x] **Step 4: Verify media properties and repository tests**

  Use `ffprobe` for duration, dimensions, codecs, and audio streams, then run
  the complete demo test suite and inspect git diff/status.

- [ ] **Step 5: Review, merge, and atomically replace the export**

  Request code review, fix material findings, push the feature branch, merge its
  pull request into `main`, verify the merged tree, and copy through a temporary
  download filename before renaming it to `Layak-VC-Walkthrough.mp4`.
